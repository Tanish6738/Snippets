// Project Member Modal
import React from 'react';

const ProjectMemberModal = ({ isOpen, onClose, onSave, member }) => {
  if (!isOpen) return null;
  return <div>Project Member Modal</div>;
};

export default ProjectMemberModal;