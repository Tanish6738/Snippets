// Task Attachment Modal
import React from 'react';

const TaskAttachmentModal = ({ isOpen, onClose, onUpload }) => {
  if (!isOpen) return null;
  return <div>Task Attachment Modal</div>;
};

export default TaskAttachmentModal;