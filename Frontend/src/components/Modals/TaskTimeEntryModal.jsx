// Task Time Entry Modal
import React from 'react';

const TaskTimeEntryModal = ({ isOpen, onClose, onSave, timeEntry }) => {
  if (!isOpen) return null;
  return <div>Task Time Entry Modal</div>;
};

export default TaskTimeEntryModal;